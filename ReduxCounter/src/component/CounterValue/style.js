import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    mainView: {
       alignSelf:"center",
        marginBottom:20
    },
    textView: {
        fontSize:60,
        color:"blue"
    }
})

export default styles;